name: eco
on:
  push:
    branches:
      - "leccion/**"
permissions: {}
jobs:
  eco:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: escribir hola y fallar
        run: |
          echo "hola" >> "$GITHUB_STEP_SUMMARY"
          exit 1
