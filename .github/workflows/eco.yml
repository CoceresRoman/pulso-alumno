name: eco
on:
  push:
    branches:
      - "leccion/**"
permissions: {}
jobs:
  eco:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: escribir hola
        run: echo "hola" >> "$GITHUB_STEP_SUMMARY"
